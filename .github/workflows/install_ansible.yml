name: Install Ansible
on: push
jobs:
  access_server:
    name: Install Ansible
    runs-on: ubuntu-latest
    steps:
      - name: Create a file
        uses: appleboy/ssh-action@master
        with:
          host: 3.110.166.178
          username: ubuntu
          key: ${{ secrets.PRIVATE_KEY }}
          script: |
            sudo apt-get update
